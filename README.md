React frontend of a fan website dedicated to Silent Hill video game series.

The app implements React Router in order to go to different pages and all the data is imported from a local `data.js` file.

NodeJS needs to be installed in order for the app to run.

To run the app locally, use the command `npm start` while inside the project directory
